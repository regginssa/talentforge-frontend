import { Icon } from "@iconify/react";

const SocialAuthButtonGroup = () => {
  return (
    <div className="flex items-center gap-6">
      <button className="rounded-full border border-black flex items-center justify-center gap-2">
        <Icon icon="mdi:apple" width={20} />
        <span className="text-sm">Continue with Apple</span>
      </button>

      <button className="rounded-full border border-blue-600 bg-"></button>
    </div>
  );
};

export default SocialAuthButtonGroup;
